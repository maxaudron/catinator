include:
  - project: "cocainefarm/templates/ci"
    file: "/rust.base.yaml"
    rules:
      - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
  - project: "cocainefarm/templates/ci"
    file: "/tanka.base.yaml"
    rules:
      - if: $CI_COMMIT_TAG

workflow:
  rules:
    - if: $CI_COMMIT_TAG
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
