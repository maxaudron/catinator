include:
  - project: "cocainefarm/templates/ci"
    file: "/rust.yaml"
    rules:
      - if: $CI_COMMIT_TAG == null
  - project: "cocainefarm/templates/ci"
    file: "/tanka.base.yaml"
    rules:
      - if: $CI_COMMIT_TAG
